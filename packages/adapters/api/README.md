# API Adapter Boundary

Official API adapters live in their platform packages. This directory documents the V1.0 boundary: API adapters must use reviewed official endpoints, capability detection, bounded retries, and the existing persistent Job Queue. They must not import browser selectors or renderer state.
