# OAuth Adapter Boundary

OAuth adapters remain in their existing platform packages and continue to use the main-process `OAuthSessionManager` and safeStorage credentials. V1.0 does not modify the existing Douyin, Facebook, YouTube, or TikTok OAuth implementations.
