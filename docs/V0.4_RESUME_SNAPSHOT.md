# V0.4 恢复现场快照

审计日期：2026-08-20（Asia/Shanghai）  
项目路径：`C:\Users\Administrator\Desktop\codex_media_publisher_starter`  
恢复原则：不重新初始化、不重新创建 starter、不重复生成平台矩阵；以当前工作区文件和可复现实验结果为准。

## 现场结论

V0.3.2 的源码现场仍然存在，DeepSeek 修复和 V0.3.1/V0.3.2 的可靠性测试保持通过。恢复执行后，V0.4 已新增抖音、快手、哔哩哔哩、YouTube、TikTok 五个独立 Adapter，并完成运行时 Registry/manifest 同步接入；真实平台验收仍未完成。V0.4 的公共数据/发布基础包括 `0004_v04_platforms.sql`、视频输入模型、OAuth 基础设施、平台 manifest/capability 约定和 Publisher 视频分支。

当前 V0.4 尚未完成全平台接入。运行时 `AdapterRegistry` 已注册 TestPlatform、微信公众号、抖音、快手、哔哩哔哩、YouTube 和 TikTok；平台能力矩阵中已有的官方调研结论继续作为唯一研究输入，不在本轮重复调研或重生成矩阵。

## 已完成

- V0.3.2 DeepSeek 连接诊断代码与测试已存在；相关测试覆盖模型可用性、Bearer 认证、超时、错误映射和 Thinking 参数。
- SQLite migration 0000–0004 源文件已存在，迁移器按文件名顺序执行并记录 `migrations` 表。
- V0.4 公共 schema 扩展已在源码中存在：视频素材、视频任务字段、发布 payload、账号授权和平台 transport/credential/source 字段。
- `PlatformAdapter`、`AdapterManifest`、能力检测、错误码、视频发布接口、状态轮询接口已在 `packages/adapters/core` 与 domain 中定义。
- `OAuthManager`、加密 CredentialStore、Playwright 会话管理和人工验证状态边界已存在。
- `PublisherService` 已把平台调用放在 Adapter 后面，并保留持久化 Job、PublishRecord、dry-run、有限重试、登录失效暂停和未知提交结果 reconciliation 逻辑。
- 抖音、快手、哔哩哔哩、YouTube、TikTok 已各自落在独立 Adapter 包，包含 manifest、capability detection、可注入网络边界、输入校验、错误映射和离线测试；未引入浏览器 selector 或验证绕过。
- 桌面主进程已注册上述五个 Adapter，并调用 `syncAdapterManifests` 将运行时能力同步到平台目录/profile；路径别名、桌面 workspace 依赖和 lockfile 已更新。
- 微信公众号 Adapter 已实现官方 API 文章草稿/dry-run、正式提交分支和 publish status 回查；含独立测试。
- TestPlatform、核心领域测试、SQLite 持久化测试、可靠性测试和 DeepSeek 测试均已存在。
- 已补充 V0.4 migration/manifest 同步集成测试，并同步配置 Vitest 的 Adapter aliases。
- 当前验证结果：`pnpm lint` 通过；`pnpm typecheck` 通过；`pnpm test` 通过，15 个测试文件、61 个测试全部通过；`pnpm build` 通过。

## 进行中

- V0.4 平台 Adapter 批次接入：首批五个平台的代码和独立测试已落地；Sol 已完成第一轮公共层审核和运行时集成。
- 快手与 B 站仍以“官方端点/字段契约需注入或确认”的 fail-closed 方式运行；TikTok 依赖官方 Content Posting API 的公网媒体 URL 与应用权限；代码完成不等于平台真实能力已验收。
- 抖音、YouTube、TikTok 的完整 OAuth 回调、账号配置 UI 和端到端凭据联调仍待完成；平台 Worker 不应修改公共层。
- 真实平台仍停留在官方权限、OAuth、应用审核、用户凭据和人工 dry-run 之前；本地 Adapter 测试不升级外部平台生命周期状态。

## 未开始

- 其他矩阵平台的 Adapter：保持 `not_implemented`、`manual_only`、`blocked` 或 `unverified`，不得用登录后 selector、逆向接口或风控绕过补齐。
- 快手/B 站官方端点契约、OAuth 回调和真实低量 dry-run；抖音/YouTube/TikTok 的完整 OAuth IPC/账号配置 UI。
- V0.4 安装包重建和安装包资源核验。
- 真实凭据联调、真实低量 dry-run、正式发布确认以及 `PublishPassed`/`Stable` 的 owner gate。

## 当前阻塞与风险

1. **Git 基线不可用**：指定目录不存在 `.git`，父级 Desktop 也没有覆盖该目录的 Git 元数据。因此本次无法提供可信的 `git status`、`git diff`、分支或 SHA；不得通过 `git init`、重置或重新初始化来补救。后续源码变更清单以文件审计和测试证据为准，恢复版本 provenance 需要 owner 提供/恢复原始仓库元数据。
2. **发布资源滞后**：源码有 `packages/db/migrations/0004_v04_platforms.sql`，但 `release/win-unpacked/resources/packages/db/migrations` 当前只包含 0000–0003。`pnpm build` 已通过，但尚未重建安装包；V0.4 交付前必须重新构建并检查安装包资源，不能把旧 V0.3.2 包当作 V0.4 验证依据。
3. **外部平台权限阻塞**：矩阵已记录的 OAuth scope、应用审核、开发者身份、账号权限、真实封面/视频和人工安全验证仍需用户/平台侧完成；遇验证码、安全验证或人工审核必须暂停并交给用户。
4. **平台能力差异风险**：文章与视频不能共享一套假设；每个 Adapter 必须通过 capability detection 和独立验证约束输入，Publisher 不得写入平台 selector 或平台流程。

## 下一步任务

1. 为快手/B 站注入并审核官方端点契约，为抖音/YouTube/TikTok 接入主进程 OAuth 回调和加密账号凭据 IPC；不把 token 暴露到 Renderer。
2. 补充跨 Adapter contract test、发布记录验证、登录失效/人工验证/有限重试验证。
3. 更新 Electron 构建/extraResources，重建 V0.4 安装包并确认安装包包含 0004 migration 与新增 Adapter 运行时文件。
4. 继续按平台 Worker 串行推进其他已有研究结论的平台；未满足官方能力与权限证据时保持 fail-closed，不重复调研。
5. 只有在用户凭据、平台审核和真实低量联调完成后，才推进对应平台 lifecycle 状态；否则明确保持 `CodeComplete`、`WaitingForUser` 或 `Developing`。

## 现场边界

- 不重新初始化项目，不重建 starter，不修改既有平台能力矩阵和路线图的研究结论。
- 不让多个 Worker 同时修改 `packages/domain`、`packages/publisher`、`packages/db` 或 `AdapterRegistry`。
- 不执行任何真实发布，不处理未授权账号，不实现验证码/安全验证/风控绕过。
