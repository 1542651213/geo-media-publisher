# V1.2.5 verification delta

## Content-kind runtime routing and live gate

- The runtime now registers both same-key Toutiao capabilities without overlap: `toutiao + video` resolves to the existing official API adapter, while `toutiao + article` resolves to `ToutiaoArticleBrowserAdapter` through `PublishJob.contentKind`. Unknown or unsupported content kinds fail closed; no registration-order fallback is used.
- Publisher execution, browser-job reconciliation/status polling, scheduler browser checks, and the desktop SELF_TEST resolver use the same content-aware route. The existing video API path remains unchanged.
- The read-only production account check found Toutiao account `3ffa4368-e8cd-4725-8658-846ad35b1980` disabled and expired, with no browser session ID and no encrypted browser-session record. The real dry-run therefore stops at `LOGIN_REQUIRED`; no article editor page gates were attempted and `finalSubmitClicks=0`.
- No new Toutiao Job, PublishRecord, external ID, or external URL exists. This round is not `READY_FOR_REAL_SELF_TEST` and does not change `PublishPassed`.

- The existing official API `toutiao` adapter remains video-only and keeps `supportsArticle=false`; this is the preserved API contract, not evidence that article publishing is complete.
- Article work is isolated in `packages/adapters/toutiao/src/browser.ts` as `ToutiaoArticleBrowserAdapter` with the same formal platform ID `toutiao`. It verifies an owned browser Session, article entry, title/body DOM, strict normalized readback, visible required fields, cover/image input, and the final-submit control.
- This module is preflight-only in this pass: final-submit click count must remain `0`, no real Job/PublishRecord/external evidence is created, and the module is not `READY_FOR_REAL_SELF_TEST` until a live owner-approved routing/session path proves all required gates.
- Zhihu uses the same conservative boundary for this pass: multi-signal editor discovery, unique-candidate enforcement, strict readback and final-control inspection are implemented; final submit remains unclicked. Existing old-record reconciliation is read-only and must not republish.
