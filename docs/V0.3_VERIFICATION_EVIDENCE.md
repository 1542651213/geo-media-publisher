# V0.3 实测证据

实测日期：2026-08-20。以下命令均在仓库根目录执行。

## 自动化检查

| 检查 | 结果 | 证据 |
|---|---|---|
| `pnpm install` | PASS | 依赖安装完成，native build 白名单包含 `better-sqlite3`、`electron`、`electron-winstaller`、`esbuild` |
| `pnpm lint` | PASS | ESLint 完成，无错误 |
| `pnpm typecheck` | PASS | TypeScript strict 检查完成，无错误 |
| `pnpm test` | PASS | 8 个测试文件，19 个测试通过；包含队列、质量门、持久化、日志脱敏、微信 Adapter 和安全存储覆盖 |
| `pnpm test:stress` | PASS | 10,000 篇文章、500 个账号、50,000 个 Job；分页返回 100 条，分页查询约 27ms，调度约 441ms |
| `pnpm build` | PASS | Electron main、preload、renderer 构建完成 |
| `pnpm installer` | PASS | Windows x64 NSIS 安装包生成完成 |

## 安装包与打包资源

- 安装包：`release/Geo Media Publisher Setup 0.3.0.exe`
- 产物大小：约 96.9 MB
- 已检查 `release/win-unpacked/resources/`：包含 `PLATFORMS.csv`、数据库 migrations、`app.asar` 和 `better-sqlite3` 解包资源。
- 本轮未自动执行安装、首次启动和 Windows 卸载回归；这些动作保留给人工验收，步骤见 [MANUAL_ACTIONS.md](../MANUAL_ACTIONS.md)。

## 平台边界

- `TestPlatform`：`Stable`，只用于本地自动化和压力测试，不计入真实平台数量。
- 微信公众号：Adapter 代码、草稿、正式提交分支和 `freepublish/get` 状态回查已实现；状态保持 `Developing`，必须提供真实 AppID/AppSecret 完成低量 Dry Run 和真实发布确认后，才能提升到 `PublishPassed`。
- 其他图文平台：继续保持 `Unverified` / `not_implemented`，没有用页面 selector、逆向接口或虚构结果填充平台数量。
