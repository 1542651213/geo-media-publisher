# Task10U Failed One-Shot Confirmation Reconciliation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restore exactly one verified orphaned Task10S confirmation to its canonical retryable pre-confirm state without creating authorization, operation, Job, Intent, PublishRecord, or publication side effects.

**Architecture:** A typed `PlatformSelfTestService.reconcileFailedOneShotConfirmation` accepts only the known XHS account and test-run identity. The repository loads a complete reconciliation snapshot, enforces the strict orphan predicate, and performs the single `publish_confirmed_at = NULL` transition plus audit-step update in one transaction; repeated calls return `ALREADY_RECONCILED` without mutation.

**Tech Stack:** TypeScript strict mode, Electron IPC/preload, better-sqlite3 transactions, Vitest, electron-vite, electron-builder.

**Spec:** User-provided TASK 10U — ONE-SHOT PARTIAL CONFIRMATION FORMAL RECONCILIATION.

## Global Constraints

- Do not modify migration 0023 unless the schema cannot support reconciliation; current schema supports the required state transition.
- Do not enter XHS, upload, fill content, confirm Owner authorization, or submit.
- Production mutation is allowed only through service → repository → transaction.
- Reject authorization, operation, Job, Intent, PublishRecord, publication, final-submit, external-evidence, NeedsReconciliation, Published, and Verified states.
- Reconciliation mutation count is at most 1 and is not a publication or final-submit count.
- Preserve Task10S/Task10T guards and all unrelated dirty/untracked worktree changes.

### Task 1: Define the strict typed reconciliation contract

**Files:**
- Modify: `packages/domain/src/types.ts`
- Modify: `packages/db/src/schema.ts`
- Test: `packages/db/src/one-shot-publication.repository.test.ts`

- [ ] Add typed identity/result/snapshot fields for the exact XHS account and test run.
- [ ] Add a failing repository test for canonical orphan reconciliation and idempotent second call.
- [ ] Add failing tests for every unsafe-state predicate.

### Task 2: Implement repository transaction

**Files:**
- Modify: `packages/db/src/repository.ts`
- Test: `packages/db/src/one-shot-publication.repository.test.ts`

- [ ] Load the exact run, authorization relationship, operation evidence, publish evidence, and counters.
- [ ] Reject any non-orphan state before mutation.
- [ ] Transactionally restore `publish_confirmed_at` to `NULL` and update the existing confirmation step to canonical waiting-for-user evidence.
- [ ] Return `RECONCILED_RETRYABLE` once and `ALREADY_RECONCILED` thereafter.

### Task 3: Add service, IPC, and diagnostics

**Files:**
- Create: `apps/desktop/src/main/one-shot-reconciliation.ts`
- Modify: `apps/desktop/src/main/platform-self-test.ts`
- Modify: `apps/desktop/src/main/ipc.ts`
- Modify: `apps/desktop/src/main/preload.ts`
- Modify: `apps/desktop/src/shared/api.ts`
- Test: `packages/adapters/core/src/platform-self-test-entry.test.ts`

- [ ] Add the service evaluator and formal repository call with no raw SQL exposure.
- [ ] Add structured start/commit/rollback/result diagnostics.
- [ ] Add a typed IPC endpoint requiring the exact identity and never invoking browser code.
- [ ] Test wrong account/platform/ambiguous identity and service idempotency.

### Task 4: Update analyzer and regression coverage

**Files:**
- Modify: `scripts/v143-xiaohongshu-task10a-evidence.helpers.ts`
- Modify: `packages/adapters/xiaohongshu/src/task10a-evidence.helpers.test.ts`

- [ ] Parse reconciliation markers and mutation count without incrementing publication/final-submit/upload counters.
- [ ] Preserve Task10R and Task10S regression assertions.

### Task 5: Verify, package, and deploy safely

**Files:**
- Create: `electron-builder-task10u.yml` (temporary, not committed)
- Create: `release-task10u-20260901-r1` (external package output)

- [ ] Run focused tests, full tests, typecheck, lint, and build.
- [ ] Package independently and statically verify Task10U plus Task10T/Task10S markers.
- [ ] Wait for normal runtime shutdown; create a unique rollback; deploy only after 0/0/NO precheck.
- [ ] Start installed package, verify migration/runtime/markers, call only the typed reconciliation endpoint for the exact orphan, and verify final DB counts.
- [ ] Stop before any XHS or real one-shot publish action.
