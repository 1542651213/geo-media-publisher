# AGENTS.md

你正在开发一个 Windows AI 多平台自媒体发布系统。

## 总体原则

1. 使用 TypeScript 严格模式。
2. 平台功能必须通过 `PlatformAdapter` 实现。
3. 禁止把平台 selector、登录流程、发布流程写入业务层。
4. 所有发布都必须经过持久化 Job Queue。
5. App 崩溃或重启后必须能够恢复未完成任务。
6. 数据库 schema 变更必须使用 migration。
7. Cookie、StorageState、API Key 等敏感信息不得明文保存。
8. Renderer 不得直接读取敏感文件。
9. 每一次真实发布必须写入 PublishRecord。
10. AI 内容必须记录 provider、model、生成时间。
11. 浏览器自动化失败不得无限重试。
12. 登录失效后暂停对应账号。
13. 遇验证码、安全验证、人机验证时，必须暂停并要求用户完成平台正常验证；不得实现绕过。
14. 新增或修改某个平台 Adapter 时，不得修改其他平台 Adapter 的业务实现。
15. Adapter 应包含独立测试。
16. 核心领域逻辑必须有单元测试。
17. 发布计划必须可预测、可复现、可审计。
18. 禁止在没有用户授权的账号上执行任何发布。
19. 优先使用平台官方 API 或官方开放能力。
20. 对平台能力做 capability detection，不要假设所有平台支持同一组功能。

## 开发方式

- 每个任务先阅读相关文件。
- 修改前先给出简短实施计划。
- 小步提交。
- 每完成一个功能必须运行 lint、typecheck、test。
- 不要为了“让测试通过”删除测试。
- 不要用 `any` 掩盖类型问题。
- 不要在代码中硬编码 API Key。
- 不要在 UI 组件中直接访问数据库。
- 不要让平台 Adapter 直接操作 React 状态。

## Definition of Done

一个任务只有在以下条件满足时才算完成：
- 功能实现
- 类型检查通过
- lint 通过
- 测试通过
- 错误状态有处理
- 日志有记录
- 不破坏现有功能
- 文档有必要更新
